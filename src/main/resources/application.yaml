spring:
  application:
    name: pas

  datasource:
    url: jdbc:postgresql://localhost:5432/pas
    username: postgres
    password: 0376thinhpG*

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect


  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri = http://localhost:9090/realms/pas
